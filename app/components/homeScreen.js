import React, { Component } from 'react'
import { StyleSheet, View } from 'react-native'

import Stopwatch from './stopwatchWidget'

class Home extends Component {
    render() {
        return (
            <Stopwatch />
        )
    }
}

export default Home